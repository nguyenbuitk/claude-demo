---
phase: quick
plan: 260324-kj3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - CLAUDE.md
  - .github/pull_request_template.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "GSD phases create feature branches instead of committing directly to main"
    - "CLAUDE.md documents the branch/PR workflow so future Claude sessions follow it"
    - "PR template provides consistent structure for pull requests"
  artifacts:
    - path: ".planning/config.json"
      provides: "branching_strategy set to phase"
      contains: "\"branching_strategy\": \"phase\""
    - path: "CLAUDE.md"
      provides: "Git workflow documentation"
      contains: "## Git Workflow"
    - path: ".github/pull_request_template.md"
      provides: "PR template for consistent PRs"
  key_links: []
---

<objective>
Adopt a feature-branch and PR workflow for this repository.

Purpose: Stop pushing directly to main. Each GSD phase will run on its own branch (e.g., `gsd/phase-02-ci-cd-pipeline`) and merge via pull request. This aligns with the Phase 2 CI/CD goals which explicitly gate on PRs.

Output: Updated GSD config, updated CLAUDE.md with workflow instructions, and a PR template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/config.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GSD config and add PR template</name>
  <files>.planning/config.json, .github/pull_request_template.md</files>
  <action>
1. Edit `.planning/config.json`: change `git.branching_strategy` from `"none"` to `"phase"`. Leave all other git config values unchanged (the `phase_branch_template` is already set to `"gsd/phase-{phase}-{slug}"` which is correct).

2. Create `.github/pull_request_template.md` with this structure:
   - `## Summary` section (1-3 bullet points describing the change)
   - `## Changes` section (list of key files changed and why)
   - `## Test plan` section (how to verify the change works)
   - `## Checklist` section with checkboxes:
     - Tests pass (`pytest`)
     - Docker build succeeds (if Dockerfile changed)
     - CLAUDE.md updated (if architecture changed)
   - Keep it concise -- this is a small learning project, not enterprise
  </action>
  <verify>
    <automated>cat .planning/config.json | python3 -c "import sys,json; c=json.load(sys.stdin); assert c['git']['branching_strategy']=='phase', f'Expected phase, got {c[\"git\"][\"branching_strategy\"]}'; print('OK: branching_strategy=phase')" && test -f .github/pull_request_template.md && echo "OK: PR template exists"</automated>
  </verify>
  <done>config.json has branching_strategy "phase"; PR template file exists at .github/pull_request_template.md</done>
</task>

<task type="auto">
  <name>Task 2: Document branch/PR workflow in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Append a new `## Git Workflow` section to the end of CLAUDE.md with the following content:

1. **Branching strategy**: Each GSD phase runs on its own branch. Branch names follow the pattern `gsd/phase-{N}-{slug}` (e.g., `gsd/phase-02-ci-cd-pipeline`). Quick tasks may commit directly to main.

2. **Pull requests**: When a phase is complete, open a PR from the phase branch to main. Use the PR template at `.github/pull_request_template.md`. PRs should be reviewed before merging.

3. **Commit conventions**: Already in use -- conventional commits with scope (e.g., `feat(01-01): add health endpoint`). Document this explicitly:
   - `feat(phase-plan):` for new features
   - `fix(phase-plan):` for bug fixes
   - `docs(phase-plan):` for documentation
   - `test(phase):` for test additions
   - `chore(phase):` for config/tooling

4. **Do NOT push directly to main** for phase work. Quick tasks (config, docs) are the exception.

Keep the section concise (no more than ~30 lines). Match the existing CLAUDE.md tone -- terse, factual, no fluff.
  </action>
  <verify>
    <automated>grep -q "## Git Workflow" CLAUDE.md && grep -q "branching" CLAUDE.md && grep -q "pull request" CLAUDE.md && echo "OK: Git Workflow section present in CLAUDE.md"</automated>
  </verify>
  <done>CLAUDE.md contains a "Git Workflow" section documenting branch strategy, PR process, and commit conventions</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; c=json.load(open('.planning/config.json')); assert c['git']['branching_strategy']=='phase'"` passes
- `.github/pull_request_template.md` exists and contains Summary, Changes, Test plan, Checklist sections
- `CLAUDE.md` contains `## Git Workflow` section with branch naming, PR process, and commit convention docs
</verification>

<success_criteria>
1. GSD config branching_strategy is "phase" (verified by JSON parse)
2. PR template exists with structured sections
3. CLAUDE.md documents the workflow so future Claude sessions know to use branches
</success_criteria>

<output>
After completion, create `.planning/quick/260324-kj3-adopt-feature-branch-and-pr-workflow-ins/260324-kj3-SUMMARY.md`
</output>
