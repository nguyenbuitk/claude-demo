---
phase: 01-dockerize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web.py
  - tests/test_web.py
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "GET /health returns HTTP 200 with JSON body {\"status\": \"ok\"}"
    - "Existing routes (/, /add, /done, /edit, /delete, /reorder) still work"
    - "Automated test for /health passes via pytest"
  artifacts:
    - path: "web.py"
      provides: "/health endpoint"
      contains: "def health"
    - path: "tests/test_web.py"
      provides: "Flask test client tests for /health"
      contains: "test_health"
  key_links:
    - from: "web.py"
      to: "tests/test_web.py"
      via: "Flask test client hitting /health"
      pattern: "client\\.get.*health"
---

<objective>
Add the `/health` endpoint to `web.py` and create `tests/test_web.py` with Flask test client tests (Wave 0 / DOC-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dockerize/01-RESEARCH.md
@.planning/phases/01-dockerize/01-VALIDATION.md

@web.py
@conftest.py
@tests/test_tasks.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /health endpoint and tests</name>
  <files>tests/test_web.py, web.py</files>
  <action>
TDD: write `tests/test_web.py` (Flask test client, 4 tests: 200 status, JSON content-type, `{"status":"ok"}` body, index still works), then make them pass by adding `jsonify` to the Flask import and a `/health` route to `web.py`.
  </action>
  <verify>
    <automated>pytest tests/test_web.py -x &amp;&amp; pytest</automated>
  </verify>
  <done>GET /health returns 200 with {"status": "ok"}. All tests green.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/01-dockerize/01-01-SUMMARY.md`
</output>
