name: Hello World Pipeline

on: [push]

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: |
          echo "Hello from GitHub Actions!"
          echo "Running on $(uname -a)"
